<div class="container">
  <div class="card">
    <div class="card-header">
      <h5> Information </h5>
    </div>
    <div class="card-body">
        <div>
        <%= @user.created_at.to_s.split('UTC')[0] %>
        </div>
        <%#=  @user.current_sign_in_at %>
        <strong> <%= @user.username %></strong>  has  <%= pluralize(@user.lections.count, "lection") %>
    </div>
  </div>
</div>
